# Track D - Logback And Runtime Logs

Read `../runtime-and-probes.md`.

Verify:

- `logback-prod.xml` uses the correct appender.
- Runtime logging flags point to the intended file.
- Health checks do not pollute request logs.
