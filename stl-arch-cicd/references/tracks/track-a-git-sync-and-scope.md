# Track A - Git Sync And Scope

Read `../core-toolchain.md` and `../core-pipelines.md` only as needed.

Verify:

- `git pull --rebase` ran before edits.
- Request scope is reduced to the minimum track set.
