# SteyApi Play CORS — expose Content-Disposition to cross-origin JS clients.
# See references/cors-exposed-headers.md
play {
  filters {
    cors {
      exposedHeaders = ["Content-Disposition"]
    }
    enabled += play.filters.cors.CORSFilter
  }
}
