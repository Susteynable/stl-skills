# Track F - CORS and Downloads

Read `../cors-exposed-headers.md`.

Verify:

- Cross-origin downloads expose `Content-Disposition`.
- Download endpoints use `.fileDownloadLogic`.
- Download responses return `ApiFileDownloadResponse`.
